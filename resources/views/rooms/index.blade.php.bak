<x-app-layout>

    <x-slot name="header">
        <div class="flex flex-col gap-1 sm:flex-row sm:items-center sm:justify-between">
            <div>
                <h2 class="text-2xl font-bold tracking-tight text-gray-900">
                    จัดการห้องพัก
                </h2>

                <p class="mt-1 text-sm text-gray-500">
                    จัดการข้อมูลห้องพักและตรวจสอบสถานะห้องพัก
                </p>
            </div>

            <div class="text-sm text-gray-500">
                ห้องพักทั้งหมด {{ $rooms->count() }} ห้อง
            </div>
        </div>
    </x-slot>


    <div class="py-6 sm:py-8">

        <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">


            {{-- =====================================================
                 แจ้งเตือน
            ====================================================== --}}

            @if (session('success'))

                <div class="mb-6 rounded-xl border border-green-200 bg-green-50 px-4 py-3 text-sm text-green-700">
                    <div class="flex items-center gap-2">
                        <span class="text-lg">✓</span>
                        <span>{{ session('success') }}</span>
                    </div>
                </div>

            @endif


            @if ($errors->any())

                <div class="mb-6 rounded-xl border border-red-200 bg-red-50 px-4 py-4 text-sm text-red-700">

                    <div class="font-semibold">
                        ⚠️ กรุณาตรวจสอบข้อมูล
                    </div>

                    <ul class="mt-2 list-disc list-inside space-y-1">

                        @foreach ($errors->all() as $error)
                            <li>{{ $error }}</li>
                        @endforeach

                    </ul>

                </div>

            @endif


            {{-- =====================================================
                 Main Card
            ====================================================== --}}

            <div class="dorm-card overflow-hidden">


                {{-- Header --}}
                <div class="border-b border-gray-200 p-5 sm:p-6">

                    <div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">

                        <div class="flex items-center gap-3">

                            <div class="flex h-11 w-11 items-center justify-center rounded-xl bg-green-50 text-green-700">

                                <svg class="h-6 w-6"
                                     fill="none"
                                     stroke="currentColor"
                                     viewBox="0 0 24 24">

                                    <path stroke-linecap="round"
                                          stroke-linejoin="round"
                                          stroke-width="2"
                                          d="M3 10.5L12 3l9 7.5M5 9.5V21h14V9.5M9 21v-6h6v6"/>

                                </svg>

                            </div>


                            <div>

                                <h3 class="text-lg font-bold text-gray-900">
                                    รายการห้องพัก
                                </h3>

                                <p class="mt-1 text-sm text-gray-500">
                                    ข้อมูลห้องพักทั้งหมดของหอพัก
                                </p>

                            </div>

                        </div>


                        <a
                            href="{{ route('rooms.create') }}"
                            class="btn-primary w-full sm:w-auto"
                        >
                            <span class="mr-2 text-lg">+</span>
                            เพิ่มห้องพัก
                        </a>

                    </div>

                </div>


                {{-- =================================================
                     Table
                ================================================== --}}

                @if ($rooms->count() > 0)

                    <div class="overflow-x-auto">

                        <table class="dorm-table">

                            <thead>

                                <tr>

                                    <th class="text-center">
                                        ห้อง
                                    </th>

                                    <th class="text-center">
                                        ชั้น
                                    </th>

                                    <th>
                                        ประเภทห้อง
                                    </th>

                                    <th class="text-right">
                                        ค่าเช่า
                                    </th>

                                    <th class="text-center">
                                        สถานะ
                                    </th>

                                    <th class="text-center">
                                        จัดการ
                                    </th>

                                </tr>

                            </thead>


                            <tbody>

                                @foreach ($rooms as $room)

                                    <tr>

                                        {{-- ห้อง --}}
                                        <td class="text-center">

                                            <span class="font-bold text-gray-900">
                                                {{ $room->room_number }}
                                            </span>

                                        </td>


                                        {{-- ชั้น --}}
                                        <td class="text-center">
                                            {{ $room->floor }}
                                        </td>


                                        {{-- ประเภทห้อง --}}
                                        <td>

                                            <span class="font-medium text-gray-800">
                                                {{ $room->room_type }}
                                            </span>

                                        </td>


                                        {{-- ค่าเช่า --}}
                                        <td class="text-right">

                                            <span class="font-semibold text-gray-900">
                                                {{ number_format($room->rent, 2) }}
                                            </span>

                                            <span class="text-xs text-gray-500">
                                                บาท
                                            </span>

                                        </td>


                                        {{-- สถานะ --}}
                                        <td class="text-center">

                                            @if ($room->status === 'ว่าง')

                                                <span class="status-success">
                                                    ● ว่าง
                                                </span>

                                            @elseif ($room->status === 'มีผู้เช่า')

                                                <span class="status-danger">
                                                    ● มีผู้เช่า
                                                </span>

                                            @elseif ($room->status === 'จอง')

                                                <span class="status-warning">
                                                    ● จอง
                                                </span>

                                            @elseif ($room->status === 'ซ่อมแซม')

                                                <span class="inline-flex items-center rounded-full bg-orange-100 px-2.5 py-1 text-xs font-semibold text-orange-700">
                                                    ● ซ่อมแซม
                                                </span>

                                            @else

                                                <span class="status-info">
                                                    {{ $room->status }}
                                                </span>

                                            @endif

                                        </td>


                                        {{-- จัดการ --}}
                                        <td>

                                            <div class="flex flex-wrap items-center justify-center gap-2">

                                                <a
                                                    href="{{ route('rooms.edit', $room->id) }}"
                                                    class="inline-flex items-center rounded-lg bg-blue-50 px-3 py-1.5 text-xs font-semibold text-blue-700 transition hover:bg-blue-100"
                                                >
                                                    ✏️ แก้ไข
                                                </a>


                                                <form
                                                    action="{{ route('rooms.destroy', $room->id) }}"
                                                    method="POST"
                                                    onsubmit="return confirm('คุณต้องการลบห้อง {{ $room->room_number }} ใช่หรือไม่?')"
                                                >

                                                    @csrf
                                                    @method('DELETE')

                                                    <button
                                                        type="submit"
                                                        class="inline-flex items-center rounded-lg bg-red-50 px-3 py-1.5 text-xs font-semibold text-red-700 transition hover:bg-red-100"
                                                    >
                                                        🗑️ ลบ
                                                    </button>

                                                </form>

                                            </div>

                                        </td>

                                    </tr>

                                @endforeach

                            </tbody>

                        </table>

                    </div>

                @else

                    {{-- =================================================
                         Empty State
                    ================================================== --}}

                    <div class="px-5 py-14 text-center sm:px-6">

                        <div class="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl bg-green-50 text-3xl">
                            🏠
                        </div>

                        <h3 class="mt-4 text-lg font-bold text-gray-900">
                            ยังไม่มีข้อมูลห้องพัก
                        </h3>

                        <p class="mt-1 text-sm text-gray-500">
                            เริ่มต้นด้วยการเพิ่มห้องพักเข้าสู่ระบบ
                        </p>

                        <a
                            href="{{ route('rooms.create') }}"
                            class="btn-primary mt-5"
                        >
                            <span class="mr-2 text-lg">+</span>
                            เพิ่มห้องพัก
                        </a>

                    </div>

                @endif


            </div>

        </div>

    </div>

</x-app-layout>